import { characters } from "./character";
import { playableCharacters } from "./playable";
import { relations } from "./relation";
import { relationMembers } from "./relation-members";
import { supportCards } from "./support";

export const db = {
  characters,
  playableCharacters,
  relations,
  relationMembers,
  supportCards,
};
